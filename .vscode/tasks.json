{
	"version": "2.0.0",
	"tasks": [
		{
			"label": "Initialize React with Vite",
			"type": "shell",
			"command": "npm create vite@latest frontend -- --template react && cd frontend && npm install && npm install -D tailwindcss postcss autoprefixer && npx tailwindcss init -p",
			"group": "build"
		}
	]
}